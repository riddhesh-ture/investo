import { Typography, Box } from "@mui/material";
import React from 'react';

function Dashboard() {
  return (
    <Box>
        <Typography variant="h4">Dashboard</Typography>
        <Typography variant="body1">this is a dashboard</Typography>
    </Box>
  )
}

export default Dashboard;